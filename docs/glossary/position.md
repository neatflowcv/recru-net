# position

개별 채용 공고 1건은 `position`으로 지칭한다.

혼란 방지를 위해 다른 표현은 표준 용어로 사용하지 않는다.

금지 표현:
- `job posting`
- `job listing`
- `job`
- `opening`
- `posting`
- `listing`

문서, 코드, 변수명, API 필드명, 설명문에서 개별 공고를 가리킬 때는 항상 `position`을 사용한다.
